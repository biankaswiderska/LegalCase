{% extends 'base.html.twig' %}

{% block body %}
    <h1>Investigationstatuses list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Value</th>
                <th>Comment</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for investigationStatus in investigationStatuses %}
            <tr>
                <td><a href="{{ path('investigationstatus_show', { 'id': investigationStatus.id }) }}">{{ investigationStatus.id }}</a></td>
                <td>{{ investigationStatus.value }}</td>
                <td>{{ investigationStatus.comment }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('investigationstatus_show', { 'id': investigationStatus.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('investigationstatus_edit', { 'id': investigationStatus.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('investigationstatus_new') }}">Create a new investigationStatus</a>
        </li>
    </ul>
{% endblock %}
