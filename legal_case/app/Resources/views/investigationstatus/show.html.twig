{% extends 'base.html.twig' %}

{% block body %}
    <h1>Status sprawy dochodzeniowej</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ investigationStatus.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ investigationStatus.value }}</td>
            </tr>
            <tr>
                <th>Komentarz</th>
                <td>{{ investigationStatus.comment }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('investigationstatus_index') }}">Powrót do listy</a>
        </li>
        <li>
            <a href="{{ path('investigationstatus_edit', { 'id': investigationStatus.id }) }}">Edytuj</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Usuń">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
