{% extends 'base.html.twig' %}

{% block body %}
    <h1>Juridicalperson</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ juridicalPerson.id }}</td>
            </tr>
            <tr>
                <th>Organizationtype</th>
                <td>{{ juridicalPerson.organizationType }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ juridicalPerson.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('juridicalperson_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('juridicalperson_edit', { 'id': juridicalPerson.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
