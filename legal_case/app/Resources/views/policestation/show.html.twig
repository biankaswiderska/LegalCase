{% extends 'base.html.twig' %}

{% block body %}
    <h1>Policestation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ policestation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ policestation.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ policestation.phone }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ policestation.comment }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('policestation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('policestation_edit', { 'id': policestation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
